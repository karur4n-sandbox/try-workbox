{
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "workbox generateSW"
  },
  "dependencies": {},
  "devDependencies": {
    "workbox-cli": "^3.6.3"
  }
}
